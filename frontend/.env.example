VITE_API_BASE='http://localhost:3001/api' # Use this URL when developing locally
# VITE_API_BASE="https://$CODESPACE_NAME-3001.$GITHUB_CODESPACES_PORT_FORWARDING_DOMAIN/api" # for GitHub Codespaces
# VITE_API_BASE='/api' # Use this URL deploying on non-localhost address OR in docker.
# VITE_PRISM_EXPERIMENTAL_SURFACES='enabled' # Enable PrismHero, PrismDodecahedron, UI Lab, Repo Lab, and manual preview tooling outside local dev.
# VITE_PRISM_REPO_LAB='enabled' # Enable Repo Lab outside local dev when experimental surfaces are also enabled.
